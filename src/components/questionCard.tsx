const QuestionCard =()=>{
    return (
        <div>
    </div>
    )
}

export default QuestionCard;

{/* <div>
<p className="number">
  question : {questionNr}/ {total_number}
</p>
<p dangerouslySetInnerHTML={{__html: question}}></p>
<div>
  {answer.map((item) => {
    return (
      <div>
        <button disabled={userAnser} onClick={callback}>
          <span dangerouslySetInnerHTML={{__html: item}}></span>
        </button>
      </div>
    );
  })}
</div>
</div> */}